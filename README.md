### To start project run the command in the root directory

```
docker-compose up -d
```

### Afer application is up you can visit next ursl:

- http://localhost:3000/
- http://localhost:4000/graphql
